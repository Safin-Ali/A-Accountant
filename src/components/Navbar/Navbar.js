import React from "react";

const Navbar = () => {
  return (
    <header>
      <nav>
        {/* Brand Logo */}
        <div>
            <img src="" alt="" />
        </div>

        {/* Route NavLink */}
        <div>
        </div>

        {/* User Profile */}
        <div>
        </div>
      </nav>
    </header>
  );
};

export default Navbar;
